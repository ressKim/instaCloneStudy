spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/studyInsta?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: instaStudy
    password: 0104qwer@

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true